import { CrearArrayPipe } from './crear-array.pipe';

describe('CrearArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new CrearArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
